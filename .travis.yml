language: node_js
node_js:
  - "0.12"

sudo: true

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq mono-devel mono-xbuild
  - npm install grunt-cli -g

addons:
  apt:
    packages:
      - gcc
